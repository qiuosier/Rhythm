branches:
  only:
  - master
language: python
cache: pip
install:
  - pip install -r requirements.txt
  - pip install coverage python-coveralls
script: coverage run --source=. -m unittest discover -s tests -p "tests_*.py"
after_success:
  - coveralls
